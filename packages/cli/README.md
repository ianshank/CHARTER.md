# charter-cli

The command-line interface for [charter-kit](https://github.com/ianshank/CHARTER.md),
plus the git and filesystem adapters that implement `charter_core`'s ports.

```console
$ charter init --profile lite
$ charter lint
$ charter check
```

Run `charter --help` for the full command surface.
